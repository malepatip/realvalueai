/**
 * Portal dashboard — placeholder page for authenticated users.
 */
export default function DashboardPage(): React.JSX.Element {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Welcome to RealValue AI.</p>
    </div>
  );
}
